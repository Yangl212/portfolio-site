# VortexNet 求职案例：已确认范围与待补材料

预览：`http://localhost:3000/project/vortexnet`

## 已确认

- 实习中实际交付并实现的是 **Today’s overview** 和旁边的任务型侧栏导航，覆盖 Monitor today、Reconcile、Review over time 三组入口。
- 个人工作范围是**信息层级与信息整合**。下游目的页及其完整流程不写成个人已实现的工作。
- 当前主面板继续做了作品集展示层面的优化，标注 **Portfolio refinement · Mock data**。它不能直接视为当时上线、交付或测试过的界面。
- 作品集后续优化包括指标定义、现金变化图、结算时间表、负责人和下一步动作；这些细节使用模拟数据。
- 目前没有确认产品或数据团队提出的具体实施限制，页面不将任何虚构约束写成真实团队决定。现有取舍只描述可见的设计结果：日常监控获得更高优先级，周期报告仍保留在命名分组中。
- 14 → 9 表示展示材料中主导航入口数量的变化，属于结构对比；不能据此推导使用效率或上线后的效果。

## 已确认的一次用户测试

用户本人补充的经历：

1. 最初认为颜色过于一致、排版较平，让信息提取较慢，先尝试从颜色上区分信息。
2. 做过一次用户测试；观察到改善有限，反馈一般。
3. 因此改变后续设计方向，调整字体、字号与层级，重新安排阅读顺序。

页面已按 **Initial hypothesis → One user test → Revised decision** 呈现。测试用于说明设计判断如何改变；没有补写参与人数、招募对象、测试任务、引语、用时、成功率，或第二轮验证结果。

指标对比图放在 “Explore a metric-hierarchy study” 中，说明它是层级研究的重建示意；不把现有两张图说成当时颜色方案的测试截图。

## 页面这轮的呈现

- 前置重建前后对比，可以切换 Before / After。
- 首屏说明个人范围和部分上线状态；正文保留任务分组、优先级与导航对比。
- 用可直接阅读的三步决策说明补足“为什么改方向”，不把关键测试经历藏在折叠区。
- 主面板围绕资金变化、清算进度和待处理队列展开；图下分别说明图表服务的判断。
- 保留局部指标展开图，随主面板更新指标定义、单位和辅助信息。
- 不恢复图片上的 View full size、模拟浏览器地址栏或绿色外围边框；Before / After 标签保留在图片外。
- 移除“4 次点击降到 0”的结果表述；现有材料只有四个路径节点，无法证明四次点击。
- 不再使用带有矛盾说明的 `region-before` 对比图。原始 HTML、截图与媒体文件保留，方便之后核对。

## 最值得补的两个材料

1. **一次真实协作决定**：产品或数据同事提出了什么约束，你有哪些选项，最后为什么采用当前信息层级。现有源材料没有填写具体刷新频率约束，暂不补写其影响。
2. **测试中的具体片段**：参与者当时要找什么，出现了什么行为，你据此调整了哪个层级。只补充记得或有记录的事实；记不起原话时使用转述。现有页面已讲清测试引起的方向变化，仍可用一个具体片段加强依据。

## 主面板重绘的核对原则

- 金额明确标币种，笔数和批次明确标单位；不把总交易数直接改名为已清算量。
- 比较使用相同的时间区间；模拟日期、更新时刻、交易记录和截止时间保持一致。
- 资金趋势使用统一货币与刻度，避免把不同量纲放进同一张图。
- 清算进度按批次计数：7 个计划批次中 4 个已结算、3 个待结算；不把收款金额误当作结算计划的完成比例。
- 待办体现优先级、负责人和下一步动作，不暗示这些动作已经完成开发或验证。
- “重构前后”展示的是设计差异；不会包装成已经测量到的效率收益。

导航对应关系仍需核对：14 个旧入口移出 4 个管理入口后仍有 10 个，新版展示为 9 个；需要确认哪一项被合并，以及旧入口到新位置的实际映射。

## 推荐补一段 6–10 秒循环视频

优先展示同一任务下的信息层级变化：旧版停留约 2 秒 → 切到新版，突出关键指标和待办区域 → 停留约 3 秒 → 回到旧版。两版使用相同窗口尺寸、统计区间和已核对的模拟数据。

如采用当前重绘版本，标注 **Portfolio refinement · Mock data**；如有允许公开的已上线片段，单独说明上线模块和个人贡献。静态截图之间的切换不包装成可交互产品录屏。

建议输出静音 MP4（H.264），保持文字清晰，循环播放。只用短标签说明前后变化，无需再添加大段字幕或视频下方控制条。

## 当前主面板的模拟快照

- 2025-10-15 12:00（UTC+8），所有金额为 CNY。
- 收款 ¥880,102，付款 ¥604,880，净现金流 +¥275,222；曲线终点与指标一致，共用零起点金额轴。
- 待结算 ¥192,400 = ¥48,200 + ¥77,000 + ¥67,200，截止时间分别为 14:00、15:00、16:00；待结算金额不计入实际现金流。
- 41 个失败转账（18 个可重试）、12 个未匹配记录、5 个审批请求属于各自队列，不合计为一个任务总数。
- 以上是本次作品集重绘使用的示例数值，不是上线数据或测试结果。

## 维护素材

- `vortexnet-source/VortexNet Case Study.html`：维护中的打包展示 HTML；新版仪表盘已写回，旧过程部分仍包含未填完的占位内容。
- `vortexnet-source/capture-tool/dashboard.cjs`：当前仪表盘的 HTML、样式、图表及模拟数据源。
- `vortexnet-source/capture-tool/refine-dashboard.cjs`：将新版仪表盘同步回打包 HTML。
- `vortexnet-source/captures/`：从源 HTML 生成的当前截图。
- `public/vortexnet/media/`：页面使用的 WebP 图片。
- `scripts/vortexnet-media-build.cjs`：输出 WebP 与首页封面。

重新生成：先运行 `node vortexnet-source/capture-tool/refine-dashboard.cjs`，再运行 `node vortexnet-source/capture-tool/capture.mjs`，最后运行 `node scripts/vortexnet-media-build.cjs`。若改变尺寸，同时更新页面图片的宽高属性。
