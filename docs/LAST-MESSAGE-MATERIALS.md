# Last Message 求职案例：本次修改与待补材料

预览：`http://localhost:3000/project/lastmessage`

## 已完成

- 首屏明确这是浏览器侦探游戏，写清角色、个人工作范围、4 个月周期和可玩的交付物。
- 根据现有录屏片尾，将项目背景标为 Parsons MFA Design & Technology 的 Thesis Project，替换原来的 Professional Work。
- 主线改为体验 → 调查流程 → AI 行为 → 测试迭代 → Figma 到实装，增加段落导航。
- 调查循环、实际 Story Map 和 Figma / 实装对比默认展开。探索研究、完整提示词和早期聊天界面保留在折叠区域，图片可打开原尺寸。
- 每个体验说明设计选择与取舍，突出信息架构、状态反馈和意外输入的处理。
- 修正第四段视频的原始错位：它展示警告、聊天断开和 Revoked 结局，不是线索解锁。
- Story Map 录屏来自结局页面；正文已明确这一点，不将它描述成录屏已证明的游戏中导航功能。
- 四段录屏保留完整桌面画面，改为轻量静音循环；点击画面暂停，无下方控制条。正文内提供原尺寸视频入口。
- 沿用原文的 8 人、约 35 分钟、2/8 找到隐藏结局，区分观察、已实施行为和待测试的改动。未新增参与者、引语或测试结果。
- 首页项目简介同步改为具体的侦探游戏、AI 对话和分支流程介绍。

## 最值得补的录屏

| 优先级 | 素材 | 建议镜头与作用 |
| --- | --- | --- |
| 1 | 一条完整的证据解锁链，12–18 秒 | 锁定成员 / 日记 → 在对话或记录中找到线索 → 完成识别 / 核对 → 返回显示解锁与新内容。让招聘方看到信息与状态如何真正连起来。现有第四段不能替代这一段。 |
| 2 | 入场流程前后对比，10–15 秒 | 同样尺寸并排呈现旧版文字入口与新版事件报告 → 玩家身份 → 首个可操作目标。只展示实际版本，不将补做的演示写成历史测试证据。 |
| 3 | 站外线索的完整回程，12–18 秒 | 游戏内线索 → X 对应帖子 → 返回游戏 → 提交 / 使用发现 → 新状态。现有第三段仅展示站外资料，没有离开与返回的过程。 |
| 4 | AI 警告的完整发生过程，12–18 秒 | 输入越界问题 → 第一次警告 → 再次输入 → 失去权限。现有第四段从已有聊天历史与断开状态开始，无法看到所有输入是如何触发反馈的。 |

建议 MP4 H.264、静音、30fps，桌面画面至少 1600px 宽。每个关键状态停留约 2 秒，结尾停顿再回到起点。保留必要的界面上下文，关闭无关标签页、通知和录屏工具条。不必另做 GIF。

## 还需要补或确认的材料

1. **封面源文件**：现有 `public/lastmessage/cover.png` 的屏幕内文字写成了 “Detctive ARG”。请在原始封面 / Figma 中改为 “Detective ARG” 后重新导出。作品集里的 HTML 文案不能修改图片内部文字。
2. **测试原始记录**：8 位玩家的任务、是否需要提示、找到主嫌疑人与隐藏结局的记录，以及约 35 分钟的统计口径（例如典型时长、均值或范围）。当前保留原文数值，没有把它包装成改版带来的提升。
3. **真实的迭代前后证据**：至少一组旧界面、实际观察、修改后界面。如果有同任务复测，再补结果。目前不能仅凭 2/8 找到隐藏结局就确定是引导不足，也可能涉及玩家选择。
4. **具体角色分工（若有合作）**：目前按原页面和片尾的 Designed & Developed Lele Yang 描述个人贡献。如有共同设计、开发或研究人员，补姓名 / 职责，才能进一步精确范围。

## 已生成资源

| 前缀 | 实际内容 | 时长 | 视频大小 |
| --- | --- | --- | --- |
| `chat` | Mike 收到 “who are you?” 并回复 | 10.43 秒 | 0.228 MB |
| `routes` | 结局页面中的 Story Map 横向查看 | 7.80 秒 | 0.276 MB |
| `web-clue` | X 上虚构角色的资料与线索帖子 | 6.70 秒 | 1.497 MB |
| `boundary` | Lily 的警告记录、断开聊天、Revoked 结局 | 16.53 秒 | 1.136 MB |

路径：`public/lastmessage/media/{前缀}-loop.mp4`，各有对应的 `-poster.webp`。

全部 1600×1000、30fps、静音 H.264、faststart。保留原速交互，结尾短暂停留并淡回开头。四段总计约 3.14 MB，原片约 28.61 MB，减少约 89%。原始录屏保留。

复现脚本：`scripts/lastmessage-media-build.cjs`。可用 `FFMPEG_PATH` 指定支持 libx264 的完整 FFmpeg 程序。
